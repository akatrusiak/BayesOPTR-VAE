copied the MCAT dtl_dragon directory

data_empty: no values included
data_web: mcat values for the default webpage initial conditions
data_file: mcat values for the default file initial conditions

UPDATE FROM OLIVIER:

 both of the files listed above are incorrect

for a stripped foil, I'd use starting parameters x=y=0.1cm (the size doesn't change), but would broaden the transverse momentum to 20mrad in each dimension; r12=r34=0
 
